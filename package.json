{
  "dependencies": {
    "@graphile-contrib/pg-simplify-inflector": "^3.0.0",
    "express": "^4.16.4",
    "postgraphile": "^4.2.0"
  },
  "devDependencies": {
    "@dillonkearns/elm-graphql": "^1.0.4",
    "elm": "^0.19.0-bugfix2"
  },
  "scripts": {
    "develop": "./server.js",
    "dump_schema": "pg_dump --schema-only --schema=public --schema=internal --schema=beatmon beatmon > sql/03_schema.sql"
  }
}
